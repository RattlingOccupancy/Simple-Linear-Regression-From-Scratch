# Simple Linear Regression from Scratch (Ordinary Least Squares – Closed-Form) 

Just a **Simple Linear Regression from scratch** 🙂 using Python.
It predicts placement package (in LPA) based on CGPA using the **OLS (Ordinary Least Squares)** method.

## Dataset
- `placement.csv` is used as the dataset.
- It contains two columns:
  - `cgpa` → input feature
  - `package` → target variable

## Requirements
- Python 3.11.8
- pandas
- numpy
- scikit-learn

Install dependencies:
```bash
pip install -r requirements.txt
